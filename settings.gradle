pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.multiplatform' version "$kotlin_plugin_version"
        id 'org.jetbrains.kotlin.jvm' version "$kotlin_plugin_version"
    }
}

rootProject.name = 'kotlin-inline-collections'

include 'benchmarks'

include 'multiplatform'
